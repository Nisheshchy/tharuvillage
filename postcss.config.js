// Tailwind CSS v4 is handled via @tailwindcss/vite plugin in vite.config.js
// No PostCSS tailwind config needed when using the Vite integration
export default {
  plugins: {
    autoprefixer: {},
  },
}
